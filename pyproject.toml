[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lumos"
version = "0.4.0"
description = "A runtime-neutral, spec-driven orchestrator for AI skills and agents."
readme = "README.md"
requires-python = ">=3.9"
license = "Apache-2.0"
license-files = ["LICENSE"]
keywords = ["ai", "agents", "orchestration", "workflow", "skills", "llm", "pipeline"]
authors = [{ name = "Lumos contributors" }]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Build Tools",
]
# The engine runs on the standard library alone. PyYAML is used when present and
# is otherwise replaced by a bundled block-style parser (see yamlcompat.py).
dependencies = []

[project.optional-dependencies]
yaml = ["PyYAML>=5.1"]
dev = ["ruff>=0.5"]

[project.urls]
Homepage = "https://github.com/sajadshafi/lumos"
Documentation = "https://github.com/sajadshafi/lumos/tree/main/docs"
Issues = "https://github.com/sajadshafi/lumos/issues"

[project.scripts]
lumos = "ai_loom.cli:main_cli"
loom = "ai_loom.cli:main_cli"

[tool.hatch.build.targets.wheel]
packages = ["src/ai_loom", "src/lumos"]

[tool.ruff]
line-length = 120
target-version = "py39"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.per-file-ignores]
# Test fixtures deliberately manipulate sys.path before importing the package.
"tests/*" = ["E402"]
# The package sets __version__ / CONTRACT_VERSION before its re-export imports.
"src/ai_loom/__init__.py" = ["E402"]
