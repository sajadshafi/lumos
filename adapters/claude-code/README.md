# Claude Code adapter

Install the CLI, then run `lumos install claude`. This copies the `/lumos` skill
to `~/.claude/skills/lumos`. Restart Claude Code and invoke `/lumos TC#123`.

The adapter drives both skills and delegated agents continuously. Project
configuration is documented in [`../../docs/configuration.md`](../../docs/configuration.md).
